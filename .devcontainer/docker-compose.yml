version: '3'
services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /usr/src/app
    volumes:
      - ..:/usr/src/app
    stdin_open: true
    tty: true
    env_file: ".env"
